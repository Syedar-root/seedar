# PRD（产品需求文档）模板

## 文档基础信息

| 项目项       | 内容                                     |
| ------------ | ---------------------------------------- |
| 文档版本     | V1.0                                     |
| 所属业务领域 | （例：电商系统/社交平台/数据分析平台等） |
| 编写人       |                                          |
| 编写日期     | YYYY-MM-DD                               |
| 审核人       |                                          |
| 审核日期     | YYYY-MM-DD                               |

## 文档目标

（简明描述：帮助目标受众（如新人/跨部门协作人员）在 30 分钟内理解「系统为什么存在」，建立业务认知框架，无需涉及技术实现、代码逻辑、开发细节等内容）

## 一、业务背景

1. 行业背景：（简述所属行业的现状、趋势或痛点）
2. 业务痛点：（明确当前业务中存在的核心问题、待解决的需求）
3. 系统价值：（说明本系统/功能解决的核心问题、带来的业务价值）

## 二、业务目标

（需具体、可衡量，避免模糊表述）

1. 核心目标：（例：上线后 3 个月内，订单履约效率提升 20%；用户留存率提升 15%）
2. 辅助目标：（例：降低人工操作成本 10%；减少用户投诉率 5%）

## 三、核心流程

### 流程图（Mermaid 语法）

```mermaid
# 示例（可根据业务场景替换，如订单流程、用户注册流程、数据分析流程等）
flowchart TD
    A[用户发起请求] --> B[系统校验规则]
    B --> C{校验通过？}
    C -- 是 --> D[执行核心业务操作]
    C -- 否 --> E[返回错误提示]
    D --> F[数据持久化]
    F --> G[返回操作结果]
```

### 流程说明

1. 关键节点说明：（逐一解释流程图中核心节点的业务逻辑、触发条件、输出结果）
2. 异常场景处理：（说明流程中异常情况的处理规则，如超时、数据异常、权限不足等）

## 四、关键业务规则

（明确、具体，避免歧义，仅聚焦业务逻辑，不涉及技术实现）

1. 核心规则：
   - 规则 1：（例：电商场景：优惠券使用规则——满 100 元可用，单笔订单限用 1 张，有效期为领取后 7 天）
   - 规则 2：（例：CRM 场景：客户分级规则——近 3 个月消费 ≥5000 元为 VIP 客户，可享受专属客服）
2. 边界规则：
   - 规则 1：（例：订单金额下限为 0.01 元，上限为 99999 元）
   - 规则 2：（例：每日用户可发起的提现操作不超过 3 次）

## 五、用户角色

| 角色名称         | 角色职责                                           |
| ---------------- | -------------------------------------------------- |
| （例：普通用户） | 发起核心业务操作、查看个人业务数据、反馈问题等     |
| （例：运营人员） | 配置业务规则、查看业务数据报表、处理用户异常诉求等 |
| （例：管理员）   | 管理用户权限、配置系统全局参数、监控核心业务指标等 |

## 六、功能范围

### 6.1 已实现功能

| 功能模块 | 功能描述 | 覆盖场景 |
| -------- | -------- | -------- |
|          |          |          |

### 6.2 未实现功能（规划中）

| 功能模块 | 功能描述 | 规划上线时间 | 优先级（高/中/低） |
| -------- | -------- | ------------ | ------------------ |
|          |          |              |                    |

## 七、成功指标

（可量化、可落地，明确统计维度和周期）
| 指标类别 | 指标名称 | 目标值 | 统计周期 | 数据来源 |
|----------------|------------------------|--------------|----------|----------------|
| 业务效率指标 | （例：订单处理耗时） | （例：≤2 小时） | 日/周/月 | （例：业务后台） |
| 业务增长指标 | （例：新增用户数） | （例：≥500 人/日） | 日/周/月 | （例：数据埋点） |
| 体验类指标 | （例：用户操作成功率） | （例：≥99%） | 日/周/月 | （例：日志统计） |

## 八、风险与限制

### 8.1 业务风险

| 风险点                       | 风险等级（高/中/低） | 影响范围     | 应对措施                             |
| ---------------------------- | -------------------- | ------------ | ------------------------------------ |
| （例：规则变更导致用户投诉） | 中                   | 核心用户群体 | 提前公示规则、灰度上线、配置回滚机制 |

### 8.2 业务限制

| 限制类型     | 具体内容                                     | 影响范围     | 备注                             |
| ------------ | -------------------------------------------- | ------------ | -------------------------------- |
| 外部依赖限制 | （例：依赖第三方支付接口，接口响应超时 ≤3s） | 支付相关功能 | 需与第三方确认 SLA，配置降级策略 |
| 资源类限制   | （例：每日可处理的最大订单量 ≤10 万单）      | 全量业务     | 高峰期需扩容，配置限流规则       |

## 附则

1. 文档更新规则：本文档随业务迭代更新，版本号按「主版本.次版本」规则调整（例：V1.1、V2.0）；
2. 解释权归属：本文档的最终解释权归「XX 业务/产品团队」所有；
3. 其他说明：（如文档适用范围、保密要求等）
