# Metric Engine V2.1 - 同环比计算功能扩展方案

## 📋 目录

- [1. 背景与目标](#1-背景与目标)
- [2. 现有架构分析](#2-现有架构分析)
- [3. 技术设计方案](#3-技术设计方案)
- [4. 详细实现步骤](#4-详细实现步骤)
- [5. 代码示例](#5-代码示例)
- [6. 测试计划](#6-测试计划)
- [7. 迁移指南](#7-迁移指南)
- [8. 风险评估与应对](#8-风险评估与应对)

---

## 1. 背景与目标

### 1.1 业务背景

同环比计算是数据分析中的核心需求，广泛应用于：

- **销售分析**：月度销售额环比增长率、年度同比对比
- **用户增长**：日活/周活/月活的周期性变化趋势
- **财务报表**：季度收入同比增长、成本环比变化
- **运营监控**：关键指标的周期性波动检测

### 1.2 当前问题

**V1 版本现状** ✅ 已实现

- `PeriodOverPeriodMetric` 类完整实现了 5 种同环比类型
- 支持 PostgreSQL、ClickHouse、MySQL 多数据库方言
- 提供百分比、绝对值、混合三种计算模式

**V2 版本现状** ❌ 缺失

- 表达式系统（`v2/expr`）无同环比表达式类型
- SQL 构建器（`v2/sql`）无法生成时间偏移查询
- 无法在 DSL 中表达同环比语义

### 1.3 目标

为 V2 版本添加完整的同环比计算能力，同时保持：

- ✅ 与现有 V2 架构的无缝集成
- ✅ 向后兼容性（不影响现有功能）
- ✅ 可扩展性（易于添加新的时间偏移类型）
- ✅ 性能优化（利用 CTE 和窗口函数）

---

## 2. 现有架构分析

### 2.1 V1 同环比实现（参考基准）

#### 核心类结构

```
src/v1/metrics/metric-classes.ts
├── PeriodOverPeriodType (枚举)          # 周期类型定义
├── PeriodOverPeriodMetricOptions (接口) # 配置选项
└── PeriodOverPeriodMetric (类)          # 主实现类
    ├── baseMetric: Metric               # 基础指标
    ├── periodType: PeriodOverPeriodType # 周期类型
    ├── calculationMode: PeriodCalculationMode # 计算模式
    ├── timeField: Field                 # 时间字段
    └── generatePeriodOverPeriodSQLExpression() # SQL 生成
```

#### 支持的周期类型（[metric-classes.ts:867-872](file:///d:/Program/projects/seedar/packages/metric_engine/src/v1/metrics/metric-classes.ts#L867-L872)）

```typescript
enum PeriodOverPeriodType {
  DAY_OVER_DAY = "日环比", // 日对日比较
  WEEK_OVER_WEEK = "周环比", // 周对周比较
  MONTH_OVER_MONTH = "月环比", // 月对月比较
  QUARTER_OVER_QUARTER = "季环比", // 季度对季度比较
  YEAR_OVER_YEAR = "年同比", // 年对年比较
}
```

#### 支持的计算模式（[core/types.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/core/types.ts)）

```typescript
enum PeriodCalculationMode {
  PERCENTAGE = "percentage", // 百分比：(当前 - 上期) / 上期 * 100%
  ABSOLUTE = "absolute", // 绝对值差：当前 - 上期
  BOTH = "both", // V1 历史模式；V2.1 core 不直接承载，由上层拆为两个指标
}
```

#### SQL 生成策略（[metric-classes.ts:993-1170](file:///d:/Program/projects/seedar/packages/metric_engine/src/v1/metrics/metric-classes.ts#L993-L1170)）

V1 采用 **子查询 + CASE WHEN** 方式：

```sql
-- 示例：月环比百分比计算
SELECT
  CASE
    WHEN (SELECT SUM(amount) FROM orders WHERE DATE_FORMAT(date, '%Y-%m') = '2023-12') = 0
       OR (SELECT SUM(amount) FROM orders WHERE DATE_FORMAT(date, '%Y-%m') = '2023-12') IS NULL
    THEN NULL
    ELSE ROUND(((SELECT SUM(amount) FROM orders WHERE DATE_FORMAT(date, '%Y-%m') = '2024-01') -
                (SELECT SUM(amount) FROM orders WHERE DATE_FORMAT(date, '%Y-%m') = '2023-12')) * 100.0 /
                (SELECT SUM(amount) FROM orders WHERE DATE_FORMAT(date, '%Y-%m') = '2023-12')), 2)
  END as mom_percentage
FROM dual
```

### 2.2 V2 现有架构

#### 表达式系统（[v2/expr](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/expr)）

```
v2/expr/
├── types.ts        # 类型定义（ExprKind, AggLevel 等）
├── ast.ts          # AST 节点类（Expr, AggExpr, BinaryExpr...）
├── parser.ts       # jsep 解析器封装
└── analyzer.ts     # 聚合层级分析器
```

**现有 ExprKind 枚举**（[types.ts:10-24](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/expr/types.ts#L10-L24)）：

```typescript
export enum ExprKind {
  Literal, // 字面量
  FieldRef, // 字段引用
  MetricRef, // 指标引用
  Call, // 函数调用
  Binary, // 二元运算
  Unary, // 一元运算
  Conditional, // 条件表达式
  Select, // 选择表达式
  In, // IN 表达式
  Between, // BETWEEN 表达式
  Like, // LIKE 表达式
  IsNull, // IS NULL 表达式
}
```

**聚合层级分析**（[analyzer.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/expr/analyzer.ts)）：

- `AggLevel.None`: 无聚合（字段、常量）
- `AggLevel.Partial`: 部分聚合（如 `SUM(a) + b`）
- `AggLevel.Full`: 完全聚合（如 `SUM(a)`、指标引用）

#### SQL 构建系统（[v2/sql](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/sql)）

```
v2/sql/
├── types.ts        # QuerySpec, JoinSpec, OrderBySpec 定义
├── knex-builder.ts # Knex 链式构建器（主入口）
└── cte-builder.ts  # CTE 自动构建器（处理 Partial 聚合）
```

**CTE 处理流程**（[cte-builder.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/sql/cte-builder.ts)）：

```typescript
// 伪代码展示 CTE 处理逻辑
buildWithCTE(spec) {
  // 1. 遍历所有指标，分离 inner/outer 层
  for (const metric of spec.metrics) {
    const aggLevel = ExprAnalyzer.getAggLevel(metric);

    if (aggLevel === Partial) {
      // 提取聚合到 inner CTE，outer 层用字段引用替换
      const {inner, outer} = this.extractAggregations(metric);
    } else if (aggLevel === Full) {
      // 直接传递到 outer 层
    }
  }

  // 2. 构建 inner CTE（执行基础聚合）
  // 3. 构建 outer 查询（组合计算）
}
```

### 2.3 差异总结

| 维度             | V1 实现                     | V2 现状       |
| ---------------- | --------------------------- | ------------- |
| **表达式类型**   | `PeriodOverPeriodMetric` 类 | ❌ 无         |
| **SQL 生成方式** | 子查询嵌套                  | ❌ 无         |
| **数据库兼容**   | PostgreSQL/ClickHouse/MySQL | ❌ 无         |
| **DSL 支持**     | 对象式 API                  | ❌ 无         |
| **CTE 集成**     | 手动管理                    | ⚠️ 有但未集成 |

---

## 3. 技术设计方案

### 3.1 设计原则

1. **最小侵入性**：新增类型和方法，不修改现有核心逻辑
2. **一致性**：遵循 V2 的表达式 AST 设计范式
3. **边界明确**：V2.1 仅支持 `QuerySpec.metrics` 顶层的同环比指标，不支持在算术/条件表达式中嵌套同环比
4. **性能优先**：V2.1 以 CTE 方案为主，窗口函数作为后续优化方向

### 3.1.1 V2.1 范围约束

为避免实现边界不清，V2.1 明确采用以下约束：

1. **顶层指标约束**：`PeriodComparisonExpr` 仅允许作为 `QuerySpec.metrics` 中的顶层指标出现，不支持 `MOM(SUM(a), t) / YOY(SUM(b), t)` 这类嵌套组合。
2. **显式时间字段约束**：`metric_engine` 内部不做时间字段推断；`PeriodComparisonExpr` 必须收到显式 `timeField`。如果上层想支持“指标默认业务时间字段”，应先在 transformer 中补齐后再传给 engine。
3. **标量结果约束**：`metric_engine` 核心层只输出标量列，因此 V2.1 仅支持 `PERCENTAGE` 和 `ABSOLUTE` 两种模式；“同时返回百分比和绝对值”由上层展开为两个独立指标实现。
4. **过滤条件继承规则**：planner 会继承所有非目标时间字段的过滤条件，只对目标时间字段上的时间 filter 做抽取与替换。comparison 分支不会原样继承 current 的时间窗口。
5. **分组支持规则**：允许与维度分组一起使用，但 current/comparison 的结果必须按维度键对齐后再计算，不允许对带维度结果直接 `CROSS JOIN`。

### 3.2 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    用户层 (User Layer)                       │
│                                                             │
│  MOM(SUM(#F92), order_date)                                 │
│  YOY(AVG(revenue), sale_date)                               │
│  WOW(COUNT(order_id), created_at)                           │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  表达式层 (Expression Layer)                  │
│                                                             │
│  ┌─────────────────┐    ┌─────────────────┐                │
│  │ PeriodComparison │◄──►│   Base Metric   │                │
│  │     Expr        │    │   (AggExpr)     │                │
│  └────────┬────────┘    └─────────────────┘                │
│           │                                                  │
│           ▼                                                  │
│  ┌─────────────────┐                                        │
│  │  ExprAnalyzer   │ ← 新增 PeriodComparison 分析逻辑       │
│  └────────┬────────┘                                        │
└───────────┼─────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────┐
│                   SQL 构建层 (SQL Layer)                     │
│                                                             │
│  ┌─────────────────┐    ┌─────────────────┐                │
│  │ KnexQueryBuilder │◄──►│   CTEBuilder    │                │
│  └────────┬────────┘    └────────┬────────┘                │
│           │                      │                          │
│           ▼                      ▼                          │
│  ┌─────────────────────────────────────────┐                │
│  │  PeriodComparisonBuilder (新模块)        │                │
│  │  - buildCurrentPeriodCTE()             │                │
│  │  - buildComparisonPeriodCTE()         │                │
│  │  - combineResults()                   │                │
│  └─────────────────────────────────────────┘                │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    输出层 (Output)                            │
│                                                             │
│  WITH current AS (...),                                     │
│       comparison AS (...)                                   │
│  SELECT (current.val - comparison.val) / comparison.val      │
│  FROM current CROSS JOIN comparison                          │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 核心数据模型设计

#### 3.3.1 时间偏移类型定义

**文件位置**: `src/v2/expr/types.ts`

```typescript
/**
 * 时间偏移类型枚举
 * 用于标识同环比计算的周期维度
 */
export enum PeriodOffsetType {
  /** 日环比 - 与前一天比较 */
  DAY_OVER_DAY = "day_over_day",

  /** 周环比 - 与上周同期比较 */
  WEEK_OVER_WEEK = "week_over_week",

  /** 月环比 - 与上月同期比较 */
  MONTH_OVER_MONTH = "month_over_month",

  /** 季环比 - 与上季度同期比较 */
  QUARTER_OVER_QUARTER = "quarter_over_quarter",

  /** 年同比 - 与去年同期比较 */
  YEAR_OVER_YEAR = "year_over_year",
}

/**
 * 计算模式枚举
 * 决定同环比结果的呈现形式
 */
export enum ComparisonMode {
  /** 百分比模式：(当前 - 上期) / 上期 * 100% */
  PERCENTAGE = "percentage",

  /** 绝对值差异模式：当前 - 上期 */
  ABSOLUTE = "absolute",
}

/**
 * 数据库方言特定的时间表达式配置
 */
export interface TimeExpressionConfig {
  /** 当前周期的开始条件 */
  currentStartExpr: string;

  /** 当前周期的结束条件 */
  currentEndExpr: string;

  /** 对比周期的开始条件 */
  comparisonStartExpr: string;

  /** 对比周期的结束条件 */
  comparisonEndExpr: string;

  /** 是否使用日期截断函数（如 DATE_TRUNC） */
  useDateTrunc?: boolean;

  /** 日期格式化模式（用于 MySQL） */
  dateFormatPattern?: string;
}
```

#### 3.3.2 同环比表达式节点

**文件位置**: `src/v2/expr/ast.ts`

```typescript
import {
  PeriodOffsetType,
  ComparisonMode,
  TimeExpressionConfig,
} from "./types";

/**
 * 同环比比较表达式
 *
 * 表示对一个基础指标进行时间维度的周期性比较，
 * 如月销售额的环比增长率、年度收入同比等。
 *
 * @example
 * // 月销售额环比增长率
 * new PeriodComparisonExpr(
 *   new AggExpr("SUM", new FieldRefExpr("amount")),
 *   PeriodOffsetType.MONTH_OVER_MONTH,
 *   ComparisonMode.PERCENTAGE,
 *   new FieldRefExpr("order_date"),  // 时间字段（必须显式提供）
 *   { alias: "sales_mom", businessName: "销售额月环比" }
 * )
 */
export class PeriodComparisonExpr extends Expr {
  /**
   * 基础指标表达式
   * 通常是一个聚合表达式（如 SUM、AVG 等）
   */
  public readonly baseMetric: Expr;

  /**
   * 时间偏移类型
   * 决定与哪个历史周期进行比较
   */
  public readonly offsetType: PeriodOffsetType;

  /**
   * 计算模式
   * 决定输出结果的格式
   */
  public readonly comparisonMode: ComparisonMode;

  /**
   * 时间字段引用
   * 由上层显式提供，engine 不负责自动推断默认时间字段
   */
  public readonly timeField: FieldRefExpr;

  /**
   * 自定义时间范围（可选）
   * 如果提供，则使用指定的时间范围而非自动推断
   */
  public readonly customTimeRange?: {
    current: { start: Date; end: Date };
    comparison: { start: Date; end: Date };
  };

  constructor(
    baseMetric: Expr,
    offsetType: PeriodOffsetType,
    comparisonMode: ComparisonMode,
    timeField: FieldRefExpr,
    customTimeRange?: PeriodComparisonExpr["customTimeRange"],
    meta?: ExprMeta,
  ) {
    super(ExprKind.PeriodComparison, meta);

    this.baseMetric = baseMetric;
    this.offsetType = offsetType;
    this.comparisonMode = comparisonMode;
    this.timeField = timeField;
    this.customTimeRange = customTimeRange;

    // 同环比结果总是完全聚合（标量值）
    this.aggLevel = AggLevel.Full;
  }

  /**
   * 克隆表达式
   * @returns 深拷贝的同环比表达式
   */
  clone(): PeriodComparisonExpr {
    return new PeriodComparisonExpr(
      this.baseMetric.clone(),
      this.offsetType,
      this.comparisonMode,
      this.timeField.clone(),
      this.customTimeRange
        ? {
            current: { ...this.customTimeRange.current },
            comparison: { ...this.customTimeRange.comparison },
          }
        : undefined,
      this.meta ? { ...this.meta } : undefined,
    );
  }

  /**
   * 获取时间偏移量的描述文本
   * @returns 中文描述（如"月环比"、"年同比"）
   */
  getOffsetDescription(): string {
    const descriptions: Record<PeriodOffsetType, string> = {
      [PeriodOffsetType.DAY_OVER_DAY]: "日环比",
      [PeriodOffsetType.WEEK_OVER_WEEK]: "周环比",
      [PeriodOffsetType.MONTH_OVER_MONTH]: "月环比",
      [PeriodOffsetType.QUARTER_OVER_QUARTER]: "季环比",
      [PeriodOffsetType.YEAR_OVER_YEAR]: "年同比",
    };
    return descriptions[this.offsetType] || "同环比";
  }

  /**
   * 获取数据库特定的配置
   * 根据当前数据库方言返回对应的时间表达式模板
   * @param dialect - 数据库方言
   * @returns 时间表达式配置对象
   */
  getTimeExpressionConfig(dialect: DatabaseDialect): TimeExpressionConfig {
    const fieldName = this.timeField.getQualifiedName();

    switch (this.offsetType) {
      case PeriodOffsetType.MONTH_OVER_MONTH:
        return this.getMonthOverMonthConfig(fieldName, dialect);

      case PeriodOffsetType.YEAR_OVER_YEAR:
        return this.getYearOverYearConfig(fieldName, dialect);

      case PeriodOffsetType.WEEK_OVER_WEEK:
        return this.getWeekOverWeekConfig(fieldName, dialect);

      case PeriodOffsetType.QUARTER_OVER_QUARTER:
        return this.getQuarterOverQuarterConfig(fieldName, dialect);

      case PeriodOffsetType.DAY_OVER_DAY:
        return this.getDayOverDayConfig(fieldName, dialect);

      default:
        throw new Error(`Unsupported period offset type: ${this.offsetType}`);
    }
  }

  // 私有方法：各种偏移类型的具体配置生成
  private getMonthOverMonthConfig(
    field: string,
    dialect: DatabaseDialect,
  ): TimeExpressionConfig {
    if (dialect === DatabaseDialect.POSTGRESQL) {
      return {
        currentStartExpr: `DATE_TRUNC('month', CURRENT_DATE)`,
        currentEndExpr: `DATE_TRUNC('month', CURRENT_DATE + INTERVAL '1 month')`,
        comparisonStartExpr: `DATE_TRUNC('month', CURRENT_DATE - INTERVAL '1 month')`,
        comparisonEndExpr: `DATE_TRUNC('month', CURRENT_DATE)`,
        useDateTrunc: true,
      };
    } else if (dialect === DatabaseDialect.MYSQL) {
      return {
        currentStartExpr: `DATE_FORMAT(CURDATE(), '%Y-%m-01')`,
        currentEndExpr: `DATE_FORMAT(DATE_ADD(CURDATE(), INTERVAL 1 MONTH), '%Y-%m-01')`,
        comparisonStartExpr: `DATE_FORMAT(DATE_SUB(CURDATE(), INTERVAL 1 MONTH), '%Y-%m-01')`,
        comparisonEndExpr: `DATE_FORMAT(CURDATE(), '%Y-%m-01')`,
        dateFormatPattern: "%Y-%m",
      };
    } else if (dialect === DatabaseDialect.CLICKHOUSE) {
      return {
        currentStartExpr: `toStartOfMonth(today())`,
        currentEndExpr: `toStartOfMonth(today() + INTERVAL 1 MONTH)`,
        comparisonStartExpr: `toStartOfMonth(today() - INTERVAL 1 MONTH)`,
        comparisonEndExpr: `toStartOfMonth(today())`,
        useDateTrunc: true,
      };
    }
    throw new Error(
      `Unsupported database dialect for month over month: ${dialect}`,
    );
  }

  private getYearOverYearConfig(
    field: string,
    dialect: DatabaseDialect,
  ): TimeExpressionConfig {
    if (dialect === DatabaseDialect.POSTGRESQL) {
      return {
        currentStartExpr: `EXTRACT(YEAR FROM ${field}) = EXTRACT(YEAR FROM CURRENT_DATE)`,
        currentEndExpr: "",
        comparisonStartExpr: `EXTRACT(YEAR FROM ${field}) = EXTRACT(YEAR FROM CURRENT_DATE - INTERVAL '12 month')`,
        comparisonEndExpr: "",
      };
    }
    // 其他数据库类似...
    throw new Error(
      `Not implemented for year over year in dialect: ${dialect}`,
    );
  }

  // ... 其他偏移类型的配置方法 ...
}
```

---

## 4. 详细实现步骤

### 4.1 Phase 1: 基础类型扩展（预计工作量：2天）

#### 步骤 1.1: 扩展 ExprKind 枚举

**文件**: `src/v2/expr/types.ts`

**操作**: 在 `ExprKind` 枚举中添加新的成员

```typescript
// 在现有枚举最后添加
export enum ExprKind {
  // ... 现有成员保持不变 ...
  IsNull = "IsNull",

  // 新增：同环比比较表达式
  PeriodComparison = "PeriodComparison",
}
```

**验证标准**:

- ✅ TypeScript 编译通过
- ✅ 不影响现有代码（向后兼容）
- ✅ 可以在 switch/case 中使用新类型

#### 步骤 1.2: 创建 PeriodComparisonExpr 类

**文件**: `src/v2/expr/ast.ts`

**操作**: 在文件末尾添加完整的类实现（参考 3.3.2 节的设计）

**关键点**:

1. 继承自 `Expr` 基类
2. 设置 `aggLevel = AggLevel.Full`
3. 实现 `clone()` 方法（深拷贝）
4. 提供 `getTimeExpressionConfig()` 方法（多数据库支持）

**验证标准**:

- ✅ 可以创建实例并访问属性
- ✅ clone() 返回独立副本
- ✅ getTimeExpressionConfig() 返回正确的 SQL 片段

#### 步骤 1.3: 导出新类型

**文件**: `src/v2/expr/index.ts`

**操作**: 更新导出列表

```typescript
export * from "./types";
export * from "./ast";
export {
  ExprParser,
  ParseContext,
  createParser,
  parseExpression,
} from "./parser";

// 新增导出
export { PeriodComparisonExpr } from "./ast";
export {
  PeriodOffsetType,
  ComparisonMode,
  TimeExpressionConfig,
} from "./types";
```

**验证标准**:

- ✅ 外部模块可以 import 新类型
- ✅ IDE 自动补全正常工作

---

### 4.2 Phase 2: 表达式分析器集成（预计工作量：1.5天）

#### 步骤 2.1: 更新 ExprAnalyzer

**文件**: `src/v2/expr/analyzer.ts`

**操作**: 在 `getAggLevel()` 方法中添加对新类型的处理

```typescript
static getAggLevel(expr: Expr): AggLevel {
  // ... 现有逻辑保持不变 ...

  // 新增：同环比表达式总是完全聚合
  if (expr instanceof PeriodComparisonExpr) {
    return AggLevel.Full;
  }

  // 兜底处理
  return AggLevel.None;
}
```

**验证标准**:

- ✅ `getAggLevel(new PeriodComparisonExpr(...))` 返回 `Full`
- ✅ 不影响其他表达式的分析结果

#### 步骤 2.2: 在 CTE 流程中透传顶层 PeriodComparisonExpr

**操作**: 在需要提取聚合子表达式的场景中识别 `PeriodComparisonExpr`，但不递归改写其内部结构。V2.1 不支持嵌套同环比，因此 CTEBuilder 只需要保证顶层 `PeriodComparisonExpr` 不会破坏现有流程。

```typescript
// 在 extractAggregations() 或类似方法中
if (currentExpr instanceof PeriodComparisonExpr) {
  // V2.1 仅支持顶层 metric，CTE 提取阶段不深入处理其内部 baseMetric
  return currentExpr;
}
```

**验证标准**:

- ✅ 顶层 `PeriodComparisonExpr` 不会破坏 CTE 的正常工作流
- ✅ 混合查询中普通指标仍按原有 CTE 规则处理

---

### 4.3 Phase 3: SQL 构建器实现（预计工作量：4天）

这是最复杂的部分，需要创建专门的 SQL 生成模块。

#### 步骤 3.1: 创建 PeriodComparisonBuilder

**新建文件**: `src/v2/sql/period-comparison-builder.ts`

**职责**:

- 接收 `PeriodComparisonExpr` 和 `QuerySpec`
- 生成包含多个 CTE 的 SQL 语句
- 支持多种数据库方言
- 抽取并替换目标时间字段上的时间 filter
- 在有维度时构建维度键对齐逻辑，避免 current/comparison 直接笛卡尔积

**关键过滤规则**:

1. planner 会继承所有非目标时间字段的过滤条件，例如 `status = 'paid'`、`shop_id = 1`、`region = '华东'`。
2. planner 会从 `spec.filters` 中识别作用于 `expr.timeField` 的时间 filter，并拆分为：
   - current 分支时间 filter：保留原查询时间窗口
   - comparison 分支时间 filter：将时间窗口按 `offsetType` 平移后替换
3. 如果找不到与 `expr.timeField` 对应的时间 filter，V2.1 直接抛错，不在 engine 内部隐式回退到 `CURRENT_DATE` 或默认 `date` 字段。

**关键分组规则**:

1. 无维度场景：current/comparison 结果可以直接 `CROSS JOIN`。
2. 有维度场景：先构建 `dimension_keys` CTE（来自 current/comparison 维度键的 `UNION`），再分别 `LEFT JOIN` current/comparison，保证各维度行一一对齐。

**核心方法设计**:

````typescript
import { Knex } from "knex";
import {
  PeriodComparisonExpr,
  PeriodOffsetType,
  ComparisonMode,
} from "../expr";
import { QuerySpec, SQLResult } from "./types";
import { DatabaseDialect } from "../../core/types";

/**
 * 同环比 SQL 构建器
 *
 * 专门用于将 PeriodComparisonExpr 转换为高效的 SQL 查询。
 * 使用 CTE（公共表表达式）分离当前周期和对比周期的计算，
 * 最后在外层查询中合并结果。
 *
 * @example 生成的 SQL 结构：
 * ```sql
 * WITH current_period AS (
 *   SELECT SUM(amount) as value
 *   FROM orders
 *   WHERE date >= '2024-01-01' AND date < '2024-02-01'
 * ),
 * comparison_period AS (
 *   SELECT SUM(amount) as value
 *   FROM orders
 *   WHERE date >= '2023-12-01' AND date < '2024-01-01'
 * )
 * SELECT
 *   (c.value - p.value) / NULLIF(p.value, 0) * 100 as percentage
 * FROM current_period c
 * CROSS JOIN comparison_period p
 * ```
 */
export class PeriodComparisonBuilder {
  private knex: Knex;

  constructor(knex: Knex) {
    this.knex = knex;
  }

  /**
   * 构建同环比查询的完整 SQL
   *
   * @param expr - 同环比表达式
   * @param spec - 查询规格（提供表信息、连接、过滤条件等）
   * @param dialect - 数据库方言
   * @returns SQL 结果对象
   */
  build(
    expr: PeriodComparisonExpr,
    spec: QuerySpec,
    dialect: DatabaseDialect,
  ): SQLResult {
    // 1. 解析过滤条件：保留非时间过滤，替换目标时间字段上的时间过滤
    const filterPlan = this.planTimeFilters(spec.filters, expr.timeField, expr.offsetType);

    // 2. 构建当前周期 CTE
    const currentCTE = this.buildPeriodCTE(
      expr,
      spec,
      filterPlan.inheritedFilters,
      filterPlan.currentTimeFilter,
      "current_period",
    );

    // 3. 构建对比周期 CTE
    const comparisonCTE = this.buildPeriodCTE(
      expr,
      spec,
      filterPlan.inheritedFilters,
      filterPlan.comparisonTimeFilter,
      "comparison_period",
    );

    // 4. 有维度时先构建维度键集合，确保 current/comparison 按维度对齐
    const dimensionKeysCTE = this.buildDimensionKeysCTE(spec, currentCTE.alias, comparisonCTE.alias);

    // 5. 组合最终查询
    const finalQuery = this.buildFinalQuery(
      currentCTE.alias,
      comparisonCTE.alias,
      dimensionKeysCTE?.alias,
      spec.dimensions,
      expr.comparisonMode,
    );

    // 6. 拼接完整 SQL
    const ctes = [currentCTE.sql, comparisonCTE.sql];
    if (dimensionKeysCTE) ctes.push(dimensionKeysCTE.sql);
    const fullSQL = `WITH ${ctes.join(",\n")}\n${finalQuery.sql}`;

    return {
      sql: fullSQL,
      bindings: [...currentCTE.bindings, ...comparisonCTE.bindings],
      columnMappings: [
        {
          alias: expr.meta?.alias || "period_comparison",
          type: "metric",
          displayName: expr.meta?.businessName || expr.getOffsetDescription(),
        },
      ],
    };
  }

  /**
   * 构建单个周期的 CTE
   *
   * @param expr - 同环比表达式
   * @param spec - 查询规格
   * @param inheritedFilters - 需要原样继承的非时间过滤条件
   * @param timeFilterSql - 当前周期或对比周期对应的时间过滤 SQL
   * @param cteName - CTE 名称
   * @returns CTE 的 SQL 和绑定参数
   */
  private buildPeriodCTE(
    expr: PeriodComparisonExpr,
    spec: QuerySpec,
    inheritedFilters: Expr[],
    timeFilterSql: string,
    cteName: string,
  ): { sql: string; alias: string; bindings: any[] } {
    const query = this.knex.queryBuilder();

    // 选择基础指标的聚合结果
    const metricSQL = this.buildMetricSQL(expr.baseMetric);
    query.select(this.knex.raw(`${metricSQL} as value`));

    // 设置主表
    query.from(`${spec.from.table} as ${spec.from.alias}`);

    // 添加连接
    for (const join of spec.joins) {
      // ... 复用现有的 join 构建逻辑 ...
    }

    // 添加时间过滤条件：current / comparison 各自使用替换后的时间过滤
    query.whereRaw(timeFilterSql);

    // 继承所有非目标时间字段的过滤条件
    for (const filter of inheritedFilters) {
      query.whereRaw(this.buildFilterSQL(filter));
    }

    // 如果有维度分组
    if (spec.dimensions && spec.dimensions.length > 0) {
      const dimColumns = spec.dimensions.map((dim) => dim.getQualifiedName());
      query.groupBy(dimColumns);
      query.select(dimColumns.map((col, i) => `${col} as dimension_${i}`));
    }

    const sqlResult = query.toSQL();

    return {
      sql: `${cteName} AS (\n${sqlResult.sql}\n)`,
      alias: cteName,
      bindings: sqlResult.bindings,
    };
  }

  /**
   * 构建最终合并查询
   *
   * @param currentAlias - 当前周期 CTE 别名
   * @param comparisonAlias - 对比周期 CTE 别名
   * @param mode - 计算模式
   * @returns 最终查询的 SQL
   */
  private buildFinalQuery(
    currentAlias: string,
    comparisonAlias: string,
    dimensionKeysAlias: string | undefined,
    dimensions: Expr[] | undefined,
    mode: ComparisonMode,
  ): { sql: string; bindings: any[] } {
    let selectClause: string;

    switch (mode) {
      case ComparisonMode.PERCENTAGE:
        selectClause = `CASE 
          WHEN ${comparisonAlias}.value = 0 OR ${comparisonAlias}.value IS NULL
          THEN NULL
          ELSE ROUND((${currentAlias}.value - ${comparisonAlias}.value) * 100.0 / ${comparisonAlias}.value, 2)
        END as percentage`;
        break;

      case ComparisonMode.ABSOLUTE:
        selectClause = `(${currentAlias}.value - ${comparisonAlias}.value) as absolute`;
        break;

      default:
        throw new Error(`Unsupported comparison mode: ${mode}`);
    }

    if (!dimensions || dimensions.length === 0) {
      return {
        sql: `SELECT ${selectClause}\nFROM ${currentAlias}\nCROSS JOIN ${comparisonAlias}`,
        bindings: [],
      };
    }

    return {
      sql: `SELECT ${this.buildDimensionSelectClause(dimensionKeysAlias!, dimensions)}, ${selectClause}
FROM ${dimensionKeysAlias}
LEFT JOIN ${currentAlias} ON ${this.buildDimensionJoinClause(dimensionKeysAlias!, currentAlias, dimensions)}
LEFT JOIN ${comparisonAlias} ON ${this.buildDimensionJoinClause(dimensionKeysAlias!, comparisonAlias, dimensions)}`,
      bindings: [],
    };
  }

  // 辅助方法：构建指标 SQL（复用现有逻辑）
  private buildMetricSQL(metric: Expr): string {
    // 这里可以调用 KnexQueryBuilder 的 buildExpr() 方法
    // 或者直接转换 AggExpr 为 SQL
    if (metric instanceof AggExpr) {
      return `${metric.functionName}(${this.buildExprSQL(metric.arg)})`;
    }
    throw new Error(`Expected aggregate expression, got: ${metric.kind}`);
  }

  // ... 其他辅助方法 ...
}
````

**验证标准**:

- ✅ 生成的 SQL 语法正确（可在数据库中执行）
- ✅ PostgreSQL/ClickHouse/MySQL 三种方言都能正确生成
- ✅ 百分比/绝对值/混合三种模式都正确
- ✅ 处理除零错误（返回 NULL）

#### 步骤 3.2: 集成到 KnexQueryBuilder

**文件**: `src/v2/sql/knex-builder.ts`

**操作**: 修改 `build()` 和 `buildSimple()` 方法，添加对 `PeriodComparisonExpr` 的特殊处理

```typescript
// 在 build() 方法中
build(spec: QuerySpec): SQLResult {
  // 检查是否有同环比表达式
  const hasPeriodComparison = spec.metrics.some(m => m instanceof PeriodComparisonExpr);

  if (hasPeriodComparison) {
    // 使用专门的 PeriodComparisonBuilder
    const periodBuilder = new PeriodComparisonBuilder(this.knex);
    // 注意：如果只有一个指标且是 PeriodComparisonExpr，直接使用
    // 如果有多个指标（包括普通指标和同环比），需要更复杂的处理
    return this.buildWithPeriodComparison(spec);
  }

  // 原有逻辑：检查是否需要 CTE
  if (this.needsCTE(spec)) {
    return this.buildWithCTE(spec);
  }
  return this.buildSimple(spec);
}

// 新增方法
private buildWithPeriodComparison(spec: QuerySpec): SQLResult {
  const periodMetrics: PeriodComparisonExpr[] = [];
  const normalMetrics: Expr[] = [];

  // 分离同环比指标和普通指标
  for (const metric of spec.metrics) {
    if (metric instanceof PeriodComparisonExpr) {
      periodMetrics.push(metric);
    } else {
      normalMetrics.push(metric);
    }
  }

  // 如果只有同环比指标
  if (normalMetrics.length === 0 && periodMetrics.length === 1) {
    const periodBuilder = new PeriodComparisonBuilder(this.knex);
    return periodBuilder.build(periodMetrics[0], spec, this.getDialect());
  }

  // 如果混合了普通指标和同环比指标（更复杂的场景）
  // 需要在 CTE 外层再包装一层
  return this.buildMixedQuery(normalMetrics, periodMetrics, spec);
}
```

**验证标准**:

- ✅ 单个同环比指标查询正常工作
- ✅ 混合查询（普通指标 + 同环比）正常工作
- ✅ 不影响原有非同环比查询的性能

#### 步骤 3.3: 集成到 CTEBuilder（可选优化）

**文件**: `src/v2/sql/cte-builder.ts`

**操作**: 当遇到 `PeriodComparisonExpr` 时，将其视为完全聚合表达式，传递到外层

```typescript
// 在 extractAggregations() 的 process() 函数中
if (currentExpr instanceof PeriodComparisonExpr) {
  // 不需要提取内部聚合，整体作为 Full 聚合处理
  return currentExpr; // 直接返回原表达式
}

// 在 buildWithCTE() 的主循环中
if (aggLevel === AggLevel.Full && expr instanceof PeriodComparisonExpr) {
  // 标记需要在后续由 PeriodComparisonBuilder 处理
  outerMetrics.push(expr);
}
```

**验证标准**:

- ✅ 包含同环比的表达式不会破坏 CTE 的正常工作流
- ✅ 性能无明显退化

---

### 4.4 Phase 4: DSL 解析器支持（预计工作量：2天）

#### 步骤 4.1: 扩展现有 ExprParser 的函数识别逻辑

**文件**: `src/v2/expr/parser.ts`

**操作**: 不新增 parser facade，也不对外暴露 `jsep` 扩展入口；直接在现有 `transformCall()` 分支中识别 `MOM/YOY/WOW/QOQ/DOD`

```typescript
/**
 * 在现有 ExprParser.transformCall() 中识别同环比函数
 * 不引入新的 parser facade，也不扩展 SQL 级 DSL。
 */
private transformCall(node: jsep.CallExpression): Expr {
  // ... 保持现有函数名和参数提取逻辑 ...

  const upperFunctionName = functionName.toUpperCase();
  const args = node.arguments.map((arg) => this.transform(arg));

  if (["MOM", "YOY", "WOW", "QOQ", "DOD"].includes(upperFunctionName)) {
    if (args.length !== 2) {
      throw new Error(`${functionName} 需要 2 个参数: baseMetric, timeField`);
    }

    const [baseMetric, timeField] = args;
    if (!(timeField instanceof FieldRefExpr)) {
      throw new Error(`${functionName} 的第二个参数必须是时间字段引用`);
    }

    return new PeriodComparisonExpr(
      baseMetric,
      PERIOD_FUNCTION_TO_OFFSET[upperFunctionName],
      ComparisonMode.PERCENTAGE,
      timeField,
    );
  }

  // ... 现有聚合函数和普通函数逻辑保持不变 ...
}
```

**支持的表达式语法示例**:

```javascript
// 基础用法
MOM(SUM(amount), order_date)
YOY(AVG(revenue), sale_date)

// 指定时间字段
WOW(COUNT(order_id), created_at)
```

**验证标准**:

- ✅ `parseExpression("MOM(SUM(amount), order_date)")` 返回 `PeriodComparisonExpr`
- ✅ 所有 5 种偏移类型都能正确解析
- ✅ 未显式提供时间字段时直接报错，不做默认回退
- ✅ 错误语法给出清晰的提示信息

---

### 4.5 Phase 5: 测试与文档（预计工作量：1.5天）

#### 步骤 5.1: 单元测试

**新建文件**: `test/test-period-comparison.spec.ts`

**测试覆盖范围**:

```typescript
describe("PeriodComparisonExpr", () => {
  describe("constructor", () => {
    it("should create instance with correct properties");
    it("should set aggLevel to Full");
    it("should handle optional customTimeRange");
  });

  describe("clone()", () => {
    it("should create independent copy");
    it("should deeply copy nested objects");
  });

  describe("getTimeExpressionConfig()", () => {
    it("should return correct config for PostgreSQL MONTH_OVER_MONTH");
    it("should return correct config for MySQL YEAR_OVER_YEAR");
    it("should return correct config for ClickHouse DAY_OVER_DAY");
    it("should throw error for unsupported dialect");
  });
});

describe("PeriodComparisonBuilder", () => {
  describe("build()", () => {
    it("should generate valid SQL for simple metric");
    it("should handle PERCENTAGE mode correctly");
    it("should handle ABSOLUTE mode correctly");
    it("should generate CTEs for both periods");
    it("should handle division by zero (return NULL)");
    it("should replace only the target time filter in comparison period");
    it("should preserve non-time filters such as status = 'paid'");
    it("should align grouped results by dimension keys");
    it("should throw when time filter for the target time field is missing");
  });

  describe("database dialects", () => {
    it("should work with PostgreSQL syntax");
    it("should work with MySQL syntax");
    it("should work with ClickHouse syntax");
  });
});

describe("Integration with KnexQueryBuilder", () => {
  it("should detect PeriodComparisonExpr and delegate to builder");
  it("should handle mixed queries (normal metrics + period comparison)");
  it("should not affect non-period-comparison queries");
  it("should reject nested period comparison expressions in V2.1");
});
```

#### 步骤 5.2: 集成测试

**场景覆盖**:

1. **简单月环比**
   - 输入: `MOM(SUM(sales_amount))`
   - 验证: SQL 正确、结果数值准确

2. **带分组的年同比**
   - 输入: `GROUP BY category, YOY(AVG(price))`
   - 验证: 每个分组都有独立的同比计算

3. **多指标混合**
   - 输入: `[SUM(quantity), MOM(SUM(revenue)), YOY(COUNT(orders))]`
   - 验证: 所有指标都能正确计算

4. **边界情况**
   - 对比周期无数据 → 返回 NULL
   - 当前周期值为 0 → 返回 0 或 NULL（根据模式）
   - 时间字段为空值 → 正确过滤

#### 步骤 5.3: 文档更新

**更新文件**:

- `docs/v2-expression-syntax.md`: 添加同环比语法说明
- `README.md`: 添加使用示例和 API 文档
- 代码注释: 为新增的类和方法添加 JSDoc

---

## 5. 代码示例

### 5.1 基础用法

```typescript
import {
  PeriodComparisonExpr,
  PeriodOffsetType,
  ComparisonMode,
  AggExpr,
  FieldRefExpr,
  KnexQueryBuilder,
  QuerySpec,
  DatabaseDialect
} from '@metric-engine';
import knex from 'knex';

// 初始化
const db = knex({ client: 'pg', connection: {...}});
const builder = new KnexQueryBuilder(db);

// 示例 1: 月销售额环比增长率
const monthlySalesGrowth = new PeriodComparisonExpr(
  new AggExpr("SUM", new FieldRefExpr("amount", "orders")),  // 基础指标：销售额求和
  PeriodOffsetType.MONTH_OVER_MONTH,                           // 月环比
  ComparisonMode.PERCENTAGE,                                  // 百分比模式
  new FieldRefExpr("order_date", "orders"),                   // 时间字段
  undefined,
  {
    alias: "sales_mom",
    businessName: "销售额月环比增长率"
  }
);

// 构建查询规格
const spec: QuerySpec = {
  from: { table: "orders", alias: "o" },
  joins: [],
  dimensions: [],
  metrics: [monthlySalesGrowth],
  filters: [
    // 可选的其他过滤条件
  ]
};

// 生成 SQL
const result = builder.build(spec);
console.log(result.sql);
/*
WITH current_period AS (
  SELECT SUM(o.amount) as value
  FROM orders o
  WHERE o.order_date >= DATE_TRUNC('month', CURRENT_DATE)
    AND o.order_date < DATE_TRUNC('month', CURRENT_DATE + INTERVAL '1 month')
),
comparison_period AS (
  SELECT SUM(o.amount) as value
  FROM orders o
  WHERE o.order_date >= DATE_TRUNC('month', CURRENT_DATE - INTERVAL '1 month')
    AND o.order_date < DATE_TRUNC('month', CURRENT_DATE)
)
SELECT
  CASE
    WHEN comparison_period.value = 0 OR comparison_period.value IS NULL
    THEN NULL
    ELSE ROUND((current_period.value - comparison_period.value) * 100.0 / comparison_period.value, 2)
  END as sales_mom
FROM current_period
CROSS JOIN comparison_period
*/
```

### 5.2 带分组的同环比

```typescript
// 示例 2: 各产品类别的年度销售同比增长
const productCategoryYoY = new PeriodComparisonExpr(
  new AggExpr("SUM", new FieldRefExpr("revenue", "sales")),
  PeriodOffsetType.YEAR_OVER_YEAR,
  ComparisonMode.ABSOLUTE,
  new FieldRefExpr("sale_date", "sales"),
  undefined,
  { alias: "revenue_yoy_diff", businessName: "年度营收同比差额" },
);

const spec: QuerySpec = {
  from: { table: "sales", alias: "s" },
  joins: [
    {
      type: "inner",
      table: "products",
      alias: "p",
      on: new BinaryExpr(
        "=",
        new FieldRefExpr("product_id", "s"),
        new FieldRefExpr("id", "p"),
      ),
    },
  ],
  dimensions: [
    new FieldRefExpr("category_name", "p", "p", {
      alias: "category",
      businessName: "产品类别",
    }),
  ],
  metrics: [
    new AggExpr("SUM", new FieldRefExpr("revenue", "s"), false, {
      alias: "total_revenue",
      businessName: "总营收",
    }),
    productCategoryYoY,
  ],
  filters: [],
};

const result = builder.build(spec);
console.log(result.sql);
/*
WITH current_period AS (
  SELECT 
    p.category_name as dimension_1,
    SUM(s.revenue) as value
  FROM sales s
  INNER JOIN products p ON s.product_id = p.id
  WHERE EXTRACT(YEAR FROM s.sale_date) = EXTRACT(YEAR FROM CURRENT_DATE)
  GROUP BY p.category_name
),
comparison_period AS (
  SELECT 
    p.category_name as dimension_1,
    SUM(s.revenue) as value
  FROM sales s
  INNER JOIN products p ON s.product_id = p.id
  WHERE EXTRACT(YEAR FROM s.sale_date) = EXTRACT(YEAR FROM CURRENT_DATE - INTERVAL '12 month')
  GROUP BY p.category_name
)
SELECT 
  c.dimension_1 as category,
  (SELECT SUM(s.revenue) FROM sales s INNER JOIN products p ON s.product_id = p.id WHERE p.category_name = c.dimension_1) as total_revenue,
  (c.value - p.value) as revenue_yoy_diff
FROM current_period c
INNER JOIN comparison_period p ON c.dimension_1 = p.dimension_1
*/
```

### 5.3 DSL 语法使用（Phase 4 实现后）

```typescript
import { parseExpression, KnexQueryBuilder } from "@metric-engine";

// 解析 DSL 表达式
const expression = parseExpression("MOM(SUM(amount))");
// expression 是 PeriodComparisonExpr 的实例

// 复杂表达式
const complexExpr = parseExpression(`
  SUM(revenue) > 1000000 
    ? YOY(AVG(profit_margin)) 
    : MOM(SUM(cost))
`);

// 在查询中使用
const spec: QuerySpec = {
  from: { table: "financial_data", alias: "f" },
  dimensions: [{ field: "quarter", alias: "q" }],
  metrics: [
    parseExpression("SUM(revenue)"),
    parseExpression("QOQ(SUM(expenses))"), // 季环比
    parseExpression("WOW(COUNT(transactions)"), // 周环比
  ],
};
```

### 5.4 与 V1 的兼容性使用（后续阶段）

> 说明：本节描述的是后续阶段的兼容方向，不属于 V2.1 核心交付范围。

```typescript
import { MetricAdapter } from "@metric-engine/compat";
import { PeriodOverPeriodMetric, PeriodOverPeriodType } from "@metric-engine";

// V1 旧代码
const v1Metric = new PeriodOverPeriodMetric(
  baseSalesMetric,
  PeriodOverPeriodType.MONTH_OVER_MONTH,
  dateField,
  PeriodCalculationMode.PERCENTAGE,
);

// 转换为 V2 表达式
const v2Expr = MetricAdapter.toExpr(v1Metric);
// v2Expr 是 PeriodComparisonExpr 的实例

// 可以继续使用 V2 的 SQL 构建器
const builder = new KnexQueryBuilder(knex);
const result = builder.build({
  from: { table: "orders", alias: "o" },
  metrics: [v2Expr],
});
```

---

## 6. 测试计划

### 6.1 单元测试（Unit Tests）

| 模块                      | 测试用例数 | 覆盖率目标       |
| ------------------------- | ---------- | ---------------- |
| `PeriodComparisonExpr`    | 15+        | 95%              |
| `PeriodComparisonBuilder` | 25+        | 90%              |
| `ExprAnalyzer` 扩展       | 5          | 100%（新增分支） |
| `KnexQueryBuilder` 集成   | 10         | 85%              |
| Parser 扩展               | 15         | 90%              |

**关键测试场景**:

1. **表达式构造**
   - ✅ 必需参数验证
   - ✅ 默认值应用
   - ✅ 元数据继承

2. **克隆行为**
   - ✅ 深拷贝独立性
   - ✅ 嵌套对象复制
   - ✅ 循环引用保护（如果有）

3. **SQL 生成正确性**
   - ✅ PostgreSQL 方言
   - ✅ MySQL 方言
   - ✅ ClickHouse 方言
   - ✅ 5 种偏移类型 × 3 种模式 = 15 种组合

4. **边界条件**
   - ✅ 除零处理
   - ✅ NULL 值传播
   - ✅ 空结果集
   - ✅ 超大数值精度

### 6.2 集成测试（Integration Tests）

| 场景                | 复杂度 | 优先级 |
| ------------------- | ------ | ------ |
| 简单单指标同环比    | P0     | 🔴 高  |
| 带维度的分组同环比  | P0     | 🔴 高  |
| 多指标混合查询      | P1     | 🟠 中  |
| 与现有 CTE 功能共存 | P1     | 🟠 中  |
| 大数据量性能测试    | P2     | 🟡 低  |
| 并发查询压力测试    | P2     | 🟡 低  |

### 6.3 回归测试（Regression Tests）

确保以下现有功能不受影响：

- ✅ 普通 AggExpr 查询（SUM/COUNT/AVG/MAX/MIN/DISTINCT_COUNT）
- ✅ 二元运算表达式（a + b, a \* b 等）
- ✅ 条件表达式（CASE WHEN）
- ✅ CTE 自动构建（Partial 聚合）
- ✅ 多表 JOIN 查询
- ✅ 过滤条件（WHERE/HAVING）
- ✅ 分页（LIMIT/OFFSET）
- ✅ 排序（ORDER BY）

### 6.4 性能基准测试

**测试环境**:

- 数据量: 100万行、1000万行、1亿行
- 数据库: PostgreSQL 14, MySQL 8.0, ClickHouse 22.x
- 查询类型: 简单同环比、分组同环比、混合查询

**性能指标**:

| 指标                     | 目标值  | 说明         |
| ------------------------ | ------- | ------------ |
| SQL 生成耗时             | < 50ms  | 不含网络 I/O |
| 小数据集查询 (<100万行)  | < 500ms | 含数据库执行 |
| 中数据集查询 (<1000万行) | < 2s    | 含数据库执行 |
| 内存占用增量             | < 10MB  | 相比 V2 基线 |

---

## 7. 迁移指南

### 7.1 从 V1 迁移到 V2.1

V2.1 的核心交付聚焦于 `metric_engine` 的 V2 执行语义，不包含 `MetricAdapter` 对 `PeriodOverPeriodMetric` 的兼容转换实现。V1 兼容迁移作为后续可选阶段（建议 Phase 6）处理。

#### 方案 A: 后续阶段通过适配器迁移（不纳入 V2.1 核心范围）

```typescript
// V1 旧代码
import {
  PeriodOverPeriodMetric,
  PeriodOverPeriodType,
  PeriodCalculationMode,
} from "@metric-engine";

const oldMetric = new PeriodOverPeriodMetric(
  baseMetric,
  PeriodOverPeriodType.MONTH_OVER_MONTH,
  timeField,
  PeriodCalculationMode.PERCENTAGE,
);

// 后续阶段的新代码（示意）
import { MetricAdapter, KnexQueryBuilder } from "@metric-engine";

const newExpr = MetricAdapter.toExpr(oldMetric);
// newExpr: PeriodComparisonExpr

// 使用 V2 的构建器
const builder = new KnexQueryBuilder(knex);
const result = builder.build({
  from: { table: "orders", alias: "o" },
  metrics: [newExpr],
});
```

#### 方案 B: 手动重写为 V2.1 原生表达式（当前推荐）

```typescript
// V1 写法
const v1Metric = new PeriodOverPeriodMetric(
  new AggregateMetric("total_sales", sumAmount, amountField),
  PeriodOverPeriodType.YEAR_OVER_YEAR,
  dateField,
  PeriodCalculationMode.PERCENTAGE,
);

// V2.1 写法（更直观）
const v2Expr = new PeriodComparisonExpr(
  new AggExpr("SUM", new FieldRefExpr("amount", "orders")),
  PeriodOffsetType.YEAR_OVER_YEAR,
  ComparisonMode.PERCENTAGE,
  new FieldRefExpr("order_date", "orders"),
  undefined,
  { alias: "sales_yoy_pct", businessName: "销售额年同比(%)" },
);

// 如需同时展示百分比和绝对值，由上层展开为两个独立指标
const v2ExprAbs = new PeriodComparisonExpr(
  new AggExpr("SUM", new FieldRefExpr("amount", "orders")),
  PeriodOffsetType.YEAR_OVER_YEAR,
  ComparisonMode.ABSOLUTE,
  new FieldRefExpr("order_date", "orders"),
  undefined,
  { alias: "sales_yoy_abs", businessName: "销售额年同比差值" },
);
```

### 7.2 新项目最佳实践

对于全新项目，建议直接使用 V2.1 API：

```typescript
// 推荐的项目结构
// src/metrics/sales-metrics.ts

import {
  PeriodComparisonExpr,
  PeriodOffsetType,
  ComparisonMode,
  AggExpr,
  FieldRefExpr,
  QuerySpec,
} from "@metric-engine";

// 预定义常用同环比指标
export const SalesMetrics = {
  // 月销售额环比
  monthlyGrowth: new PeriodComparisonExpr(
    new AggExpr("SUM", new FieldRefExpr("amount")),
    PeriodOffsetType.MONTH_OVER_MONTH,
    ComparisonMode.PERCENTAGE,
    new FieldRefExpr("order_date"),
    undefined,
    { alias: "sales_mom_pct", businessName: "销售额月环比(%)" },
  ),

  // 年营收同比
  yearlyGrowth: new PeriodComparisonExpr(
    new AggExpr("SUM", new FieldRefExpr("revenue")),
    PeriodOffsetType.YEAR_OVER_YEAR,
    ComparisonMode.PERCENTAGE,
    new FieldRefExpr("fiscal_year"),
    undefined,
    { alias: "revenue_yoy_pct", businessName: "年度营收同比(%)" },
  ),

  yearlyGrowthAbs: new PeriodComparisonExpr(
    new AggExpr("SUM", new FieldRefExpr("revenue")),
    PeriodOffsetType.YEAR_OVER_YEAR,
    ComparisonMode.ABSOLUTE,
    new FieldRefExpr("fiscal_year"),
    undefined,
    { alias: "revenue_yoy_abs", businessName: "年度营收同比差值" },
  ),

  // 周订单量环比
  weeklyOrdersChange: new PeriodComparisonExpr(
    new AggExpr("COUNT", new FieldRefExpr("id")),
    PeriodOffsetType.WEEK_OVER_WEEK,
    ComparisonMode.ABSOLUTE,
    new FieldRefExpr("created_at"),
    undefined,
    { alias: "orders_wow_diff", businessName: "订单量周变化" },
  ),
};

// 使用示例
export function buildSalesDashboardQuery(): QuerySpec {
  return {
    from: { table: "orders", alias: "o" },
    dimensions: [
      new FieldRefExpr("region", "o", "o", {
        alias: "region_code",
        businessName: "区域",
      }),
    ],
    metrics: [
      SalesMetrics.monthlyGrowth,
      SalesMetrics.yearlyGrowth,
      SalesMetrics.yearlyGrowthAbs,
      SalesMetrics.weeklyOrdersChange,
    ],
    filters: [
      // 可选的业务过滤条件
    ],
  };
}
```

### 7.3 渐进式迁移策略

如果你的项目很大，可以采用渐进式迁移：

1. **阶段一**（第1周）：安装 V2.1，在新页面/功能中使用
2. **阶段二**（第2-3周）：使用适配器逐步转换旧代码
3. **阶段三**（第4周）：全面切换到原生 V2.1 API
4. **阶段四**（第5周）：移除 V1 依赖，清理废弃代码

---

## 8. 风险评估与应对

### 8.1 技术风险

| 风险                                         | 概率 | 影响 | 应对措施                                                                        |
| -------------------------------------------- | ---- | ---- | ------------------------------------------------------------------------------- |
| **SQL 性能问题**（大数据量下 CTE 慢）        | 中   | 高   | 1. 提供窗口函数备选方案<br>2. 添加查询计划分析工具<br>3. 文档中明确性能边界     |
| **数据库方言兼容性**（某些数据库不支持 CTE） | 低   | 中   | 1. 检测数据库能力<br>2. 降级为子查询方案<br>3. 维护方言支持矩阵                 |
| **与现有 CTE 逻辑冲突**                      | 中   | 中   | 1. 充分的集成测试<br>2. 代码审查重点检查此部分<br>3. 提供开关控制是否启用新功能 |
| **表达式复杂度爆炸**（多层嵌套同环比）       | 低   | 低   | 1. V2.1 明确禁止嵌套同环比<br>2. parser/build 阶段直接报错<br>3. 文档说明该限制 |

### 8.2 业务风险

| 风险                                      | 概率 | 影响 | 应对措施                                                                    |
| ----------------------------------------- | ---- | ---- | --------------------------------------------------------------------------- |
| **计算结果不一致**（V1 vs V2.1 数值差异） | 中   | 高   | 1. 对照测试矩阵<br>2. 提供数值对比工具<br>3. 明确舍入规则（ROUND vs FLOOR） |
| **学习成本**（团队不熟悉新 API）          | 中   | 低   | 1. 详尽的文档和示例<br>2. 内部培训 session<br>3. IDE 代码片段模板           |
| **迁移期间服务中断**                      | 低   | 高   | 1. 特性开关（Feature Flag）<br>2. 灰度发布策略<br>3. 完整的回滚方案         |

### 8.3 应急预案

**如果发现严重 Bug**:

1. **立即回滚**: 使用 Git revert 回退到上一个稳定版本
2. **临时禁用**: 通过配置项关闭同环比功能，降级为基础指标查询
3. **热修复**: 发布 patch version (如 2.1.1)，仅修复紧急问题

**配置项示例**:

```typescript
// src/config.ts
export const FeatureFlags = {
  enablePeriodComparison: process.env.ENABLE_PERIOD_COMPARISON !== "false",
  maxNestingDepth: parseInt(process.env.MAX_PERIOD_NESTING_DEPTH || "2"),
  preferredSQLStrategy: process.env.PERIOD_SQL_STRATEGY || "cte", // 'cte' | 'window_function' | 'subquery'
};
```

---

## 9. 时间规划（参考）

| 阶段        | 内容           | 预计工时             | 依赖关系               |
| ----------- | -------------- | -------------------- | ---------------------- |
| **Phase 1** | 基础类型扩展   | 2 天                 | 无                     |
| **Phase 2** | 分析器集成     | 1.5 天               | Phase 1 完成           |
| **Phase 3** | SQL 构建器实现 | 4 天                 | Phase 2 完成           |
| **Phase 4** | DSL 解析器支持 | 2 天                 | Phase 1 完成（可并行） |
| **Phase 5** | 测试与文档     | 1.5 天               | Phase 3 & 4 完成       |
| **总计**    |                | **11 天（约 2 周）** |                        |

**里程碑**:

- ✅ **Day 3**: 核心类型就绪，可以通过 TypeScript 编译
- ✅ **Day 7**: SQL 生成基本可用，可通过单元测试
- ✅ **Day 9**: DSL 语法可用，开发者体验良好
- ✅ **Day 11**: 全部完成，可以发布 Beta 版本

---

## 10. 成功标准

### 10.1 功能完整性

- ✅ 支持 5 种时间偏移类型（日/周/月/季/年）
- ✅ 支持 2 种计算模式（百分比/绝对值）
- ✅ 支持 3 种主流数据库（PostgreSQL/MySQL/ClickHouse）
- ✅ 支持与维度分组和普通业务过滤条件共存
- ✅ comparison 分支会替换目标时间字段上的时间 filter，并继承其余 filters
- ✅ V2.1 范围内不引入隐式时间字段推断和 JSON 结果列

### 10.2 质量 标准

- ✅ 单元测试覆盖率 ≥ 90%
- ✅ 集成测试全部通过
- ✅ 无已知 Critical/High 级别 Bug
- ✅ 性能基线测试达标
- ✅ 代码审查通过（至少 2 人 approve）

### 10.3 用户体验

- ✅ API 设计直观易用（学习曲线 < 2 小时）
- ✅ DSL 语法简洁（≤ 80 字符表达常见场景）
- ✅ 错误提示清晰友好
- ✅ 文档完整（API 参考 + 5 个以上实战示例）
- ✅ IDE 支持完善（类型推导、自动补全、跳转定义）

---

## 附录 A: 参考资源

### 相关代码文件

- V1 实现: [src/v1/metrics/metric-classes.ts:778-1170](file:///d:/Program/projects/seedar/packages/metric_engine/src/v1/metrics/metric-classes.ts#L778-L1170)
- V2 表达式类型: [src/v2/expr/types.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/expr/types.ts)
- V2 AST 节点: [src/v2/expr/ast.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/expr/ast.ts)
- V2 分析器: [src/v2/expr/analyzer.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/expr/analyzer.ts)
- V2 SQL 类型: [src/v2/sql/types.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/sql/types.ts)
- V2 Knex 构建器: [src/v2/sql/knex-builder.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/sql/knex-builder.ts)
- V2 CTE 构建器: [src/v2/sql/cte-builder.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/v2/sql/cte-builder.ts)
- 核心类型定义: [src/core/types.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/core/types.ts)
- 兼容层: [src/compat/metric-adapter.ts](file:///d:/Program/projects/seedar/packages/metric_engine/src/compat/metric-adapter.ts)

### 外部依赖

- [Knex.js](http://knexjs.org/) - SQL Query Builder
- [jsep](https://github.com/soney/jsep) - JavaScript Expression Parser
- PostgreSQL / MySQL / ClickHouse 文档

---

## 附录 B: 术语表

| 术语   | 英文                     | 解释                      |
| ------ | ------------------------ | ------------------------- |
| 同环比 | Period-over-Period (PoP) | 时间序列数据的周期性比较  |
| 环比   | Month-over-Month (MoM)   | 与相邻上一周期比较        |
| 同比   | Year-over-Year (YoY)     | 与去年同期比较            |
| CTE    | Common Table Expression  | 公共表表达式（WITH 子句） |
| AST    | Abstract Syntax Tree     | 抽象语法树                |
| DSL    | Domain Specific Language | 领域特定语言              |
| 方言   | Dialect                  | 数据库特定的 SQL 语法变体 |

---

**文档版本**: 1.0
**创建日期**: 2026-04-11
**作者**: AI Assistant
**状态**: 待评审
