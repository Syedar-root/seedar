export enum AiStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum AiType {
  CHAT = 'chat',
  COMPLETION = 'completion',
  EMBEDDING = 'embedding',
}

export { AiSessionType, AiSessionStatus } from './enums';
