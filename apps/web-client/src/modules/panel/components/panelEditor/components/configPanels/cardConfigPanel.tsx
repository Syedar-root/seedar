import type { ConfigPanelProps } from "../../types";

export const CardConfigPanel: React.FC<ConfigPanelProps> = ({
  fields: _fields,
  metrics: _metrics,
  config: _config,
  onChange: _onChange,
}) => {
  return null;
};
