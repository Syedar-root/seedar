# CLI 模块概览

## 1. 模块职责

CLI 负责：

- 安装运行时
- 启停服务
- 升级版本
- 查看状态与日志
- 诊断问题
- 卸载与清理

## 2. 推荐使用方式

生产环境推荐入口是：

```bash
npx @syedar/seedar-cli@latest install
```

而不是直接在仓库里运行老的 PowerShell 脚本。

## 3. 为什么它重要

它把部署细节从“开发仓库知识”变成“用户可执行命令”，这对交付和运维很关键。
